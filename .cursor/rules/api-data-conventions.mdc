---
description: API、JSON、SQL、远程模型与测试契约规范
globs: **/*.{java,cs,sql,json,yml,yaml}
alwaysApply: false
---

# API / 数据规范

- JSON 字段使用 `camelCase`；枚举使用稳定的小写字符串，不暴露 Java/C# 类型名。
- HTTP 路径使用小写 kebab-case；资源使用复数名词。
- SSE 和 WebSocket 消息字段保持一致；错误必须包含稳定的 `code`、`messageKey`、参数和可选本地化 `message`。
- 服务端不要把某种语言的展示句子当作协议唯一真相；客户端根据当前应用语言解析 `messageKey`，优先展示服务端已本地化的 `message`。
- 异步请求必须有 request ID、取消语义和幂等边界；不得向客户端返回异常堆栈。
- SQL 关键字大写；表名、字段名、索引名使用 `snake_case`。
- 表名使用复数；主键为 `id`；外键为 `<table>_id`。
- 查询必须使用参数绑定，禁止拼接用户输入。
- Schema 变化必须通过可追踪迁移完成。
- 单元测试名称描述行为和条件；覆盖空输入、超时、取消和错误映射。测试默认不提交到仓库，仅本机保留。
- API 测试覆盖状态码、JSON 契约、SSE 完成/错误事件和 WebSocket 断线行为。

## 领域约定

- `chatProvider`：`local` | `remote`；远程配置资源为 `/api/remote-models`，响应不得包含明文 API Key。
- 本地模型启停走状态/模型 API，并携带预设 id；运行中修改配置应返回明确冲突类 `code`（如 `MODEL_RUNNING`）。
- 设置体中的端口、硬件设备、联网总开关等字段变更需可立即生效或明确提示需重启的边界。
- 监控快照等可选指标缺失时降级返回“不可用”结构，禁止因空指针导致 500。
