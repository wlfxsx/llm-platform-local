---
description: 项目通用代码格式、命名、安全与运行边界
alwaysApply: true
---

# 通用代码规范

- 源文件统一 UTF-8、LF 换行、末尾换行；禁止 Tab，代码 4 空格，JSON/YAML/Markdown 2 空格。
- 标识符、API 字段、数据库字段使用英文；用户可见文本必须进入中文/英文资源。
- 项目自有代码注释、配置注释、SQL 注释、脚本注释和文档内注释全部使用简体中文；注释说明“为什么”，不重复代码。
- 第三方依赖或法律要求保留的原始许可证注释不得擅自改写；新增项目注释仍必须使用中文。
- 禁止保留无效代码和无理由的 analyzer suppress。
- 永久代码、文档、脚本和配置不得放入 `.me/`。
- 不吞异常；错误必须转换为明确的领域错误或统一错误响应。
- 日志使用参数化/结构化写法，不记录令牌、密码、API Key、模型隐私输入或完整凭据。
- 文件、进程、HTTP、SSE、WebSocket 和数据库资源必须正确释放，并设置超时、取消和错误边界。
- 测试不得依赖公网；外部网络能力只能通过显式配置的 Tool/MCP/Provider/远程模型。
- JSON 字段使用 `camelCase`；SSE/WS 消息统一优先使用 `type`、`requestId`、`payload`。
- REST 用于非流式请求，SSE 用于服务端单向流式输出，WebSocket 用于双向交互和中间事件。
- 平台 API 只监听 `127.0.0.1`；不要开放外部入站端口。
- 本机推理端口（llamafile 等）避免 `8080`/`3000` 等常见端口；默认与迁移以非常用端口为准（如 `17891`），占用时自动换空闲端口。
- 远程 API Key 等密钥只写入操作系统凭据库（`SecretStore`），禁止明文写入 SQLite、配置文件、日志或仓库。
- 关闭应用时必须停止本机 Java 控制面、llamafile 及由其拉起的后台进程，避免资源残留。
- Git 提交信息统一使用中文，格式为 `类型：中文摘要`，类型使用“新增、修复、重构、文档、测试、构建、维护”等。
- README 默认使用中文写入 `README.md`，并同步维护英文版本 `readme/README-EN.md`；两者内容变化必须保持一致。
- 用户可见的提示、错误、确认、进度和状态消息必须使用中英文资源，不得硬编码。
- 运行时消息通过当前应用语言选择资源；语言切换后新消息立即使用新语言，历史消息按原始语言或资源键重新渲染。
