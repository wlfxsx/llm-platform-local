---
description: .me 仅作开发期本地临时目录，禁止当作产品运行时路径
alwaysApply: true
---

# `.me` 开发目录

- 任何不应上传到 GitHub 的本机内容，必须放入 `.me/` 对应子目录；不要把本地专用文件放在项目根目录、`platform-server/` 或 `desktop/`。
- `.me/script/` 放开发临时脚本，`.me/sql/` 放试验 SQL，`.me/files/` 放下载的二进制，`.me/model/` 放本地模型，`.me/log/` 放日志，`.me/config/` 放本机配置和密钥，`.me/temp/` 放生成物与调试草稿；构建产物放 `.me/build/`。
- 本地 SQLite、插件、RAG 索引、Skill、MCP 配置和用户数据使用 `.me/` 仅作开发机临时数据；正式运行时使用各平台用户数据目录（如 `~/.llm-platform`）。
- 可共享、可复现、构建必需的源代码、配置、迁移、规则和锁文件应放在正常项目路径并提交；不要借 `.me/` 隐藏应上传的项目文件。
- 不确定是否应上传时，先放入 `.me/temp/`，确认后再移动到正式路径。
- `.me/` 全部内容不得提交到仓库，也不得打进安装包；根目录 `.gitignore` 必须忽略整个 `.me/`，并兜底忽略密钥、数据库、模型权重、日志等误放文件。
- 测试代码（如 `**/src/test/`）默认不上传，仍放在正常项目路径便于本机跑测，不要塞进 `.me/`。
- 文档引用 `.me/` 时必须标注“仅开发机”，不得写入默认运行配置。
- `.cursor/rules/` 可提交；`.cursor/plans/` 为本地计划，不提交。
